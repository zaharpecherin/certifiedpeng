= form_tag subscribers_path do
  %article
    - if flash[:error].present?
      #error_explanation
        %p= flash[:error]
    %label.amount
  %script.stripe-button{"data-amount" => "1000", "data-description" => "A month's subscription on CertifiedPeng", "data-key" => Rails.configuration.stripe[:publishable_key], "data-locale" => "auto", :src => "https://checkout.stripe.com/checkout.js"}
